export class SoundPoint {
    //constructor(public mail: string){}
    
    constructor(public temperatur: number,public salinity: number, public depth: number,public celerity: number, public posX: number, public posY: number) {
    }
  }